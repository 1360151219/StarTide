# 《星潮守望者》Codex 迭代规则

本文件适用于项目根目录及全部子目录。任何涉及界面、美术、动画、音频、资源或视觉验收的任务，都必须先读取并遵守这里引用的当前规范。

## 1. 必读顺序

按任务范围读取，不得仅凭参考图或历史记忆实施：

1. `docs/ART_DIRECTION.md`：视觉命题、形状、材质、颜色、字体、角色保留与禁用模式。
2. `docs/UI_SPEC.md`：画布、网格、安全区、页面布局、组件状态、动效与验收值。
3. `docs/ASSET_MANIFEST.md`：当前允许存在和复用的正式素材。
4. `docs/VISUAL_DIRECTIONS.md`：为什么采用方向 D，以及方向边界。
5. `docs/VISUAL_AUDIT.md`：当前问题、优先级和值得保留的部分。

需要生成素材时，再读取对应提示词：

- 卡牌主体：`.prompts/card-art.md`
- 状态图标：`.prompts/status-icons.md`
- 背景与环境：`.prompts/backgrounds.md`
- UI 构件：`.prompts/ui-ornaments.md`

## 2. 决策优先级

出现冲突时按以下顺序处理：

1. 当前玩法、数据、稳定内容 ID、存档和音频 Cue 契约。
2. `ART_DIRECTION.md` 的视觉原则与禁用模式。
3. `UI_SPEC.md` 的布局、可读性、安全区和交互约束。
4. `ASSET_MANIFEST.md` 的素材路径与复用边界。
5. 三张参考图的构图、材质和氛围。

参考图是实现目标，不是运行时整图素材。可以尽可能还原静态层级、比例、形状与材质，但不得把角色、敌人、关卡状态、文字、数字、按钮状态或特效烘焙进背景，也不得为了像参考图而改变 Vampire Survivors-like 玩法。

## 3. 不可变产品边界

- 保持竖屏、俯视、开放地图、自动攻击；玩家唯一持续操作是移动。
- 保持 3 个自动技能槽、三选一升级和现有关卡外主流程。
- 不新增攻击键、技能键、闪避键、卡牌出牌区或封闭竞技场。
- 保留现有英雄与怪物的头身比、五官、武器比例和标志性色块。
- 不因视觉改造随意修改内容 ID、存档 Schema、数值、掉落或音频 Cue ID。

## 4. 唯一视觉方向

当前唯一方向是 D「日光远征动画」：清透赛璐璐、自然手绘、轻量远征装备材质和读战优先。

- 材质只使用浅色帆布、桦木、搪瓷、磨砂金属扣、织带、绳结和少量旧地图纸。
- UI 颜色必须来自 `ART_DIRECTION.md` 和 `UI_SPEC.md` 的语义令牌。
- 每页只允许一个日照金主操作；危险红只表达真实危险。
- 角色、敌人、危险、拾取物和伤害反馈优先于环境装饰。
- 技能必须依靠轮廓、轨迹和命中节奏区分，不能只换颜色。
- 品质必须依靠边框层数、徽章、挂扣和揭示节奏区分，不能只换颜色。

禁止 SaaS 后台、白色卡片海、通用网页按钮、统一大圆角、玻璃拟态、紫色科技渐变、满屏发光、剪纸影戏、人偶关节、暗色幕布、写实 3D 塑料、Emoji 正式图标和混用不同 AI 渲染风格。

## 5. 资源规则

- 优先复用 `docs/ASSET_MANIFEST.md` 中的现有正式素材和共享程序组件。
- 新增、替换或删除正式素材时，同步更新 `ASSET_MANIFEST.md`。
- 生成图不得包含文字、伪文字、数字、品质、按钮文案、Logo 或水印。
- 生成源图、色键图、图集母版、中间图、普通审阅截图、构建包和导出目录不得进入项目源文件。
- `docs/references/` 只允许 `battle-reference.png`、`reward-reference.png`、`map-reference.png` 三张正式审阅图。
- `preview/` 只提交 `.gdignore` 和 `responsive/*.png` 四张响应式基线；其他截图均为可再生成输出。
- 不创建重复素材、仅换色版本、`final2` 文件或第二套相同职责的目录。

## 6. 实现规则

- 视觉令牌、边框、按钮、Glyph 和布局规则必须有单一来源，不在页面复制 StyleBox 或颜色常量。
- 关卡与内容来自 `content/*.tres`、`levels/*.tres` 和目录类，不为界面维护第二份展示数据。
- 局外英雄选择只通过当前角色页与档案中的活动英雄完成，不恢复旧英雄选择/培养副本。
- 静态背景只承载环境；可交互、可变化和需要本地化的内容由 Godot 节点绘制。
- 新组件先明确职责和状态边界；能扩展现有共享组件时，不新增平行组件。
- 不在纯审计、报告或方案任务中擅自修改游戏逻辑。

## 7. 视觉验收

完成视觉改动后至少执行与改动相称的检查：

- 实际运行并截取受影响页面，不以编辑器静态预览代替。
- 检查 540×960、540×1170、540×1200、720×960 四档画布和 Safe Area。
- 对照三张参考图检查视觉焦点、材质、比例和信息层级。
- 检查 200 ms 暴露、灰度辨识、最小字号 14 px、最小触控区 48×48 px。
- 战斗需确认玩家、敌人、危险、拾取物、3 个自动技能槽和移动摇杆均清楚。
- 运行 `./tools/run_tests.sh`；视觉基线只在人工确认改动正确后更新。
- 检查运行时没有引用 `docs/references/`、`preview/` 或任何生成源文件。

## 8. 规范维护

设计文档固定为：

```text
docs/
├── VISUAL_AUDIT.md
├── VISUAL_DIRECTIONS.md
├── ART_DIRECTION.md
├── UI_SPEC.md
├── ASSET_MANIFEST.md
└── references/
    ├── battle-reference.png
    ├── reward-reference.png
    └── map-reference.png

.prompts/
├── card-art.md
├── status-icons.md
├── backgrounds.md
└── ui-ornaments.md
```

不要为一次迭代新增报告、历史方案副本或平行规范。方向、颜色、字体、形状、材质或资源路径发生变化时，直接更新上述单一来源，并同步修正引用。
